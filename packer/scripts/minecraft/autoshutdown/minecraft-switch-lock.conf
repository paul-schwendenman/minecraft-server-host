# Lock held by `minecraftctl world switch` while it runs; autoshutdown.sh skips
# its check while it's held. Created at boot so autoshutdown (running as
# minecraft, which can't create files in /run) always has it to lock.
f /run/minecraft-switch.lock 0644 root root -
